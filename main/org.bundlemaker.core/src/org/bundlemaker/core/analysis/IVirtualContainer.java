package org.bundlemaker.core.analysis;

/**
 * <p>
 * Defines a 'virtual' artifact container. Virtual containers are use for all artifact nodes that have no counter-part
 * in the underlying resource model. One example for such artifacts are missing types (and the packages that contains
 * these missing types).
 * </p>
 * 
 * @author Gerd W&uuml;therich (gerd@gerd-wuetherich.de)
 */
public interface IVirtualContainer {

  // empty tag interface
}
